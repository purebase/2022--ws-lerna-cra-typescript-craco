{
  "name": "@purebase/common",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.tsx",
  "scripts": {
    "start": "SET BROWSER=none && SET PORT=3001 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}

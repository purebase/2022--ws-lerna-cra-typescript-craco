{
  "name": "@purebase/web",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.tsx",
  "scripts": {
    "start": "SET BROWSER=none && SET PORT=3000 && craco start",
    "build": "craco build",
    "test": "craco test",
    "eject": "craco eject"
  }
}
